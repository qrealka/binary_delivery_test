cmake_minimum_required(VERSION 2.8.2)
project(${BIN_DELIVERY_ARGS_PROJ}-external NONE)

include(ExternalProject)

ExternalProject_Add(${BIN_DELIVERY_ARGS_PROJ}-external
        URL "${BIN_DELIVERY_ARGS_URL}"
        PREFIX "${BIN_DELIVERY_ARGS_PREFIX}"
        INSTALL_DIR "${BIN_DELIVERY_ARGS_PREFIX}"
		SOURCE_DIR "${BIN_DELIVERY_ARGS_DOWNLOAD_DIR}"
        CMAKE_ARGS -DCMAKE_INSTALL_PREFIX="${BIN_DELIVERY_ARGS_PREFIX}"
        CONFIGURE_COMMAND ""
        PATCH_COMMAND ""
        INSTALL_COMMAND ""
        BUILD_COMMAND ""
        UPDATE_COMMAND ""
        TEST_COMMAND ""
        )




